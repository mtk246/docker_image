version: '3.3'

services:
  app:
    image: "nextjs"
    container_name: "nextjs"
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - 4100:4100

networks:
  server:
    external: true
